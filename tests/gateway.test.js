import test from "node:test";
import assert from "node:assert/strict";
import { HatpGateway } from "../src/hatpGateway.js";
test("sends AP2 requests through the public HATP adapter",async()=>{let captured;const gateway=new HatpGateway({baseUrl:"http://localhost:8080",apiKey:"hatp_test_prefix_secret",allowInsecureHttp:true,fetchImpl:async(url,options)=>{captured={url,options};return new Response(JSON.stringify({decisionId:"d1",decision:"ALLOW",reason:"POLICY_ALLOWED"}),{status:200,headers:{"content-type":"application/json"}});}});const result=await gateway.authorizePurchase({transaction:{id:"t1"}},"req-1");assert.equal(captured.url,"http://localhost:8080/api/v1/adapters/ap2/authorize");assert.equal(captured.options.headers["x-request-id"],"req-1");assert.equal(result.decision,"ALLOW");});
test("rejects insecure non-local HATP endpoints",()=>{assert.throws(()=>new HatpGateway({baseUrl:"http://example.com",apiKey:"x",allowInsecureHttp:true}),/HATP_HTTPS_REQUIRED/);});
test("rejects malformed authorization responses",async()=>{const gateway=new HatpGateway({baseUrl:"http://localhost:8080",apiKey:"x",allowInsecureHttp:true,fetchImpl:async()=>new Response(JSON.stringify({decision:"MAYBE"}),{status:200})});await assert.rejects(()=>gateway.authorizePurchase({}),/HATP_INVALID_DECISION_RESPONSE/);});
